enum TypeScriptOperation {
    INIT,
    SET_SCRIPT_IS_OPEN,
    ADD_FILE,
    UPDATE_FILE,
    EDIT_FILE,
    REMOVE_FILE,
    GET_REFERENCES,
    GET_COMPLETIONS,
    GET_DEFINITIONS
}

export = TypeScriptOperation;
